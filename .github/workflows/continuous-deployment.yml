name: Java CD

on:
  push:
    branches:
      - master

jobs:
  deploy:
    name: Deploy Application to Server
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Pull Docker image
        run: docker pull javeexd/gamestore-app

#      - name: Set up SSH
#        uses: webfactory/ssh-agent@v0.5.3
#        with:
#          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

#      - name: Copy docker-compose.yml to server
#        run: |
#          scp -o StrictHostKeyChecking=no docker-compose.yml ${USER_TODEPLOY}@${HOST_TODEPLOY}:/home/ubuntu/gamestore/

#      - name: Remove old container and images
#        run: |
#          ssh u${USER_TODEPLOY}@${HOST_TODEPLOY} "cd /home/ubuntu/gamestore/ && docker-compose down gamestore-app && docker rmi javeexd/gamestore-app"

#      - name: Pull Docker images
#        run: |
#          ssh ${USER_TODEPLOY}@${HOST_TODEPLOY} "cd /home/ubuntu/gamestore/ && docker-compose pull"

#      - name: Run Docker Compose
#        run: |
#          ssh ${USER_TODEPLOY}@${HOST_TODEPLOY} "cd /home/ubuntu/gamestore/ && docker-compose up -d javeexd/gamestore-app"
#       name: Deploy to Render
#        if: github.ref == 'refs/heads/master'
#        env:
#          deploy_url: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
#        run: |
#          curl -X POST $deploy_url